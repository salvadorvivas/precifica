# Precifica

Calculadora de honorários e POPs internos — Salvador Vivas Advocacia.

App estático em `index.html`, hospedado no Cloudflare Workers: https://precifica.salvadorvivas.workers.dev

Deploy: `cp index.html dist/ && wrangler deploy`
